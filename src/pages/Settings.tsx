import { Card, CardContent, Typography } from "@mui/material";

export default function Settings() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5">Settings</Typography>
        <Typography color="text.secondary">Configurazioni applicazione.</Typography>
      </CardContent>
    </Card>
  );
}
